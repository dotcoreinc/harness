{
  nixantic.sources.harnesses.instructions."rules/planning" = {
    heading = "Planning tool";
    harnesses = [ "claude" ];
    content = ''
      Never use the `EnterPlanMode` tool to kick-off planning. I have my work workflow, don't use your internal one.
    '';
  };
}
